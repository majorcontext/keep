# docs/content Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Write the documentation for majorcontext.com/keep. Markdown files with YAML frontmatter, following the style guide in `docs/STYLE-GUIDE.md`.

**Architecture:** Markdown files in `docs/content/` organized into four sections: getting-started, concepts, guides, reference. Files use numeric prefixes for ordering. Frontmatter provides title, description, keywords. Content is auto-hosted on majorcontext.com.

**Tech Stack:** Markdown, YAML frontmatter.

**Depends on:** All other sub-projects (for accuracy). Can be started in parallel -- stub pages early, fill in details as features land.

---

### Task 1: Getting started section

**Files:**
- Create: `docs/content/getting-started/01-introduction.md`
- Create: `docs/content/getting-started/02-installation.md`
- Create: `docs/content/getting-started/03-first-policy.md`
- Create: `docs/content/getting-started/04-mcp-relay.md`

- [ ] **Step 1: Write introduction**

`01-introduction.md`: What Keep is, the problem it solves, the three components (engine, relay, gateway). Brief comparison table (without Keep vs. with Keep). Link to VISION.md concepts. One page, ~300 words.

- [ ] **Step 2: Write installation**

`02-installation.md`: `go install` for all three binaries. Prerequisites (Go 1.25+). Verify installation with `keep version`. TK: Homebrew when available.

- [ ] **Step 3: Write first policy**

`03-first-policy.md`: Create a rules directory, write a simple Linear rule file, run `keep validate`, write a fixture, run `keep test`. Walkthrough from zero to passing test. Include full file contents and expected output.

- [ ] **Step 4: Write MCP relay quickstart**

`04-mcp-relay.md`: Take the Linear rules from the previous page, add a relay config, start the relay, point an agent at it. Verify a tool call is evaluated. TK: may need a mock MCP server for the walkthrough.

- [ ] **Step 5: Commit**

```bash
git add docs/content/getting-started/
git commit -m "docs: add getting started section"
```

---

### Task 2: Concepts section

**Files:**
- Create: `docs/content/concepts/01-policy-engine.md`
- Create: `docs/content/concepts/02-expressions.md`
- Create: `docs/content/concepts/03-deny-audit-tune.md`
- Create: `docs/content/concepts/04-integrations.md`
- Create: `docs/content/concepts/05-defense-in-depth.md`

- [ ] **Step 1: Write policy engine concept**

`01-policy-engine.md`: The Call object, how evaluation works (scope lookup, rule ordering, short-circuit, redaction accumulation). The AuditEntry. Stateless evaluation (except rateCount). ~500 words, no step-by-step instructions.

- [ ] **Step 2: Write expressions concept**

`02-expressions.md`: Why CEL, what's available (field access, comparison, collections, string functions, custom functions). Null safety. Expression limits. Profile aliases. Not a reference (that's in reference/03) -- focus on building the mental model.

- [ ] **Step 3: Write deny/audit/tune concept**

`03-deny-audit-tune.md`: The workflow. Start in audit_only, review logs, switch to enforce. Why no human-in-the-loop approval. Harness engineering. How failure is a signal. ~400 words.

- [ ] **Step 4: Write integrations concept**

`04-integrations.md`: When to use which integration (MCP relay, LLM gateway, library). The normalization model -- how each translates its protocol into Keep calls. Cardinality differences. Direction (bidirectional for LLM).

- [ ] **Step 5: Write defense-in-depth concept**

`05-defense-in-depth.md`: Keep (API-level) + Moat (network-level) + agentsh (syscall-level). What each layer catches. Why you might want all three. Brief, not a sales pitch.

- [ ] **Step 6: Commit**

```bash
git add docs/content/concepts/
git commit -m "docs: add concepts section"
```

---

### Task 3: Guides section

**Files:**
- Create: `docs/content/guides/01-linear-mcp.md`
- Create: `docs/content/guides/02-slack-mcp.md`
- Create: `docs/content/guides/03-llm-gateway.md`
- Create: `docs/content/guides/04-library-usage.md`
- Create: `docs/content/guides/05-testing-policies.md`
- Create: `docs/content/guides/06-profiles-packs.md`

- [ ] **Step 1: Write Linear MCP guide**

`01-linear-mcp.md`: Full rule file for Linear. Profile. Starter pack. Relay config. Step-by-step: write rules, validate, test with fixtures, run relay. Expected output at each step.

- [ ] **Step 2: Write Slack MCP guide**

`02-slack-mcp.md`: Same structure for Slack. Focus on content filtering (broadcast mentions, sensitive terms).

- [ ] **Step 3: Write LLM gateway guide**

`03-llm-gateway.md`: Anthropic gateway setup. Secret redaction rules. Destructive command blocking. Request + response filtering. Expected output for redacted vs. blocked calls. TK: flesh out once gateway is implemented.

- [ ] **Step 4: Write library usage guide**

`04-library-usage.md`: Go code example -- import engine, load rules, evaluate inline. The Gmail agent pattern from the PRD. Show call construction, evaluation, handling deny/redact.

- [ ] **Step 5: Write testing policies guide**

`05-testing-policies.md`: Fixture file format. Writing good test cases (cover allow, deny, redact). Running `keep test`. Using tests in CI. Examples of edge cases worth testing.

- [ ] **Step 6: Write profiles and packs guide**

`06-profiles-packs.md`: Creating a profile for a new API. Creating a starter pack. Using overrides (disabled, when replacement, message override). Sharing packs.

- [ ] **Step 7: Commit**

```bash
git add docs/content/guides/
git commit -m "docs: add guides section"
```

---

### Task 4: Reference section

**Files:**
- Create: `docs/content/reference/01-cli.md`
- Create: `docs/content/reference/02-rule-files.md`
- Create: `docs/content/reference/03-expressions.md`
- Create: `docs/content/reference/04-relay-config.md`
- Create: `docs/content/reference/05-gateway-config.md`
- Create: `docs/content/reference/06-audit-log.md`

- [ ] **Step 1: Write CLI reference**

`01-cli.md`: Every command (`keep validate`, `keep test`), every flag, exit codes. Lookup table format.

- [ ] **Step 2: Write rule files reference**

`02-rule-files.md`: Complete schema. Every field with type, default, and description. Scope, mode, on_error, packs, rules, match, action, redact. Name format constraints. Evaluation order.

- [ ] **Step 3: Write expressions reference**

`03-expressions.md`: Every CEL function, operator, type. Input variables. Custom functions with full signatures. Standard functions. Examples for each. Expression limits.

- [ ] **Step 4: Write relay config reference**

`04-relay-config.md`: Complete `keep-mcp-relay.yaml` schema. Every field. Auth types. Log config.

- [ ] **Step 5: Write gateway config reference**

`05-gateway-config.md`: Complete `keep-llm-gateway.yaml` schema. Every field. Decompose options. Provider values.

- [ ] **Step 6: Write audit log reference**

`06-audit-log.md`: JSON schema for audit entries. Every field. Example entries for allow, deny, redact. How params_summary is computed.

- [ ] **Step 7: Commit**

```bash
git add docs/content/reference/
git commit -m "docs: add reference section"
```
